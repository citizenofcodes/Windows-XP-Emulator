<Window x:Class="FileExplorer.Explorer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileExplorer"
        mc:Ignorable="d"
        Title="Explorer" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="61*"></ColumnDefinition>
            <ColumnDefinition Width="339*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Click="BackButtonClick"> Back</Button>
        <TextBox x:Name="UrlTextBox" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" FontSize="16" Text="C:\\" TextAlignment="Left" KeyDown="UrlTextBox_KeyDown"></TextBox>

        <ScrollViewer Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">
        <WrapPanel x:Name="ExplorerGrid"  Background="Gray" ></WrapPanel>
        </ScrollViewer>
    </Grid>
</Window>
